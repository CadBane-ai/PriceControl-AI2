# BMAD (local only)
.bmad-core/
.bmad-*/

# Node/PNPM
node_modules/
# Allow lockfiles in sub-packages for Vercel builds
pnpm-lock.yaml
!apps/**/pnpm-lock.yaml

# Next.js
.next/
.vercel/
out/

# Build artifacts
dist/
coverage/

# Env files (do not commit secrets)
.env
.env.*
